import type { backendInterface, BusinessProfile, DashboardStats, Customer, Product, Invoice, PaginatedResult, PaginatedResult_1, PaginatedResult_2, StockUpdate, InvoiceStatus, PaymentStatus, UserRole } from "../backend";
import type { Principal } from "@icp-sdk/core/principal";

const now = BigInt(Date.now()) * BigInt(1_000_000);
const oneDay = BigInt(86_400_000_000_000);

const sampleProfile: BusinessProfile = {
  businessId: "aaaaa-aa" as unknown as Principal,
  name: "Sharma Fashion & Cosmetics",
  email: "contact@sharmafashion.in",
  phone: "9876543210",
  address: "42, Lajpat Nagar, New Delhi - 110024",
  category: "Retail",
  gstNumber: "07AABCS1234B1ZX",
  currency: "INR",
  language: "en",
  logoUrl: undefined,
};

const dashboardStats: DashboardStats = {
  todaySales: BigInt(23549200),         // ₹2,35,492
  monthlySales: BigInt(184291000),      // ₹18,42,910
  outstandingPayments: BigInt(53298000),// ₹5,32,980
  inventoryValue: BigInt(96750000),     // ₹9,67,500
  totalCustomers: BigInt(128),
  lowStockCount: BigInt(5),
};

const sampleProducts: Product[] = [
  {
    productId: BigInt(1),
    name: "Cotton Kurti - Royal Blue",
    category: "Clothing",
    sku: "KUR-001",
    barcode: "8901234567890",
    purchasePrice: BigInt(39900),
    sellingPrice: BigInt(69900),
    quantity: BigInt(25),
    unit: "Piece",
    taxPercent: BigInt(18),
    imageUrl: undefined,
    supplierName: "Jaipur Textile Mills",
    createdAt: now - oneDay * BigInt(10),
  },
  {
    productId: BigInt(2),
    name: "Silk Saree - Banarasi",
    category: "Clothing",
    sku: "SAR-001",
    barcode: "8901234567891",
    purchasePrice: BigInt(249900),
    sellingPrice: BigInt(449900),
    quantity: BigInt(8),
    unit: "Piece",
    taxPercent: BigInt(18),
    imageUrl: undefined,
    supplierName: "Varanasi Silk House",
    createdAt: now - oneDay * BigInt(8),
  },
  {
    productId: BigInt(3),
    name: "Men's Formal Shirt - White",
    category: "Clothing",
    sku: "SHT-001",
    barcode: "8901234567892",
    purchasePrice: BigInt(59900),
    sellingPrice: BigInt(99900),
    quantity: BigInt(3),
    unit: "Piece",
    taxPercent: BigInt(18),
    imageUrl: undefined,
    supplierName: "Mumbai Garments Co",
    createdAt: now - oneDay * BigInt(6),
  },
  {
    productId: BigInt(4),
    name: "Lakme Foundation - Beige",
    category: "Cosmetics",
    sku: "COS-001",
    barcode: "8901234567893",
    purchasePrice: BigInt(34900),
    sellingPrice: BigInt(54900),
    quantity: BigInt(2),
    unit: "Piece",
    taxPercent: BigInt(18),
    imageUrl: undefined,
    supplierName: "Lakme Distributors Delhi",
    createdAt: now - oneDay * BigInt(4),
  },
  {
    productId: BigInt(5),
    name: "Himalaya Face Wash - Neem",
    category: "Cosmetics",
    sku: "COS-002",
    barcode: "8901234567894",
    purchasePrice: BigInt(9900),
    sellingPrice: BigInt(14900),
    quantity: BigInt(40),
    unit: "Piece",
    taxPercent: BigInt(18),
    imageUrl: undefined,
    supplierName: "Himalaya Wholesale Hub",
    createdAt: now - oneDay * BigInt(2),
  },
];

const sampleCustomers: Customer[] = [
  {
    customerId: BigInt(1),
    name: "Priya Sharma",
    phone: "9876543210",
    email: "priya.sharma@gmail.com",
    address: "42, Lajpat Nagar, New Delhi - 110024",
    gstNumber: undefined,
    totalPurchaseAmount: BigInt(519800),
    pendingAmount: BigInt(0),
    createdAt: now - oneDay * BigInt(30),
  },
  {
    customerId: BigInt(2),
    name: "Rajesh Gupta",
    phone: "9845012345",
    email: "rajesh.gupta@yahoo.com",
    address: "15, MG Road, Bangalore - 560001",
    gstNumber: "29AABCU9603R1ZX",
    totalPurchaseAmount: BigInt(449900),
    pendingAmount: BigInt(449900),
    createdAt: now - oneDay * BigInt(20),
  },
  {
    customerId: BigInt(3),
    name: "Anita Patel",
    phone: "9765432109",
    email: "anita.patel@hotmail.com",
    address: "7, CG Road, Ahmedabad - 380009",
    gstNumber: undefined,
    totalPurchaseAmount: BigInt(99900),
    pendingAmount: BigInt(0),
    createdAt: now - oneDay * BigInt(15),
  },
];

const sampleInvoices: Invoice[] = [
  {
    invoiceId: BigInt(1),
    invoiceNumber: "INV-0001",
    customerId: BigInt(1),
    customerName: "Priya Sharma",
    customerPhone: "9876543210",
    items: [
      { productId: BigInt(1), productName: "Cotton Kurti - Royal Blue", quantity: BigInt(2), unitPrice: BigInt(69900), discount: BigInt(0), taxPercent: BigInt(18), lineTotal: BigInt(139800) },
      { productId: BigInt(5), productName: "Himalaya Face Wash - Neem", quantity: BigInt(4), unitPrice: BigInt(14900), discount: BigInt(0), taxPercent: BigInt(18), lineTotal: BigInt(59600) },
    ],
    subtotal: BigInt(199400),
    cgst: BigInt(17946),
    sgst: BigInt(17946),
    discount: BigInt(0),
    total: BigInt(235292),
    notes: "Thank you for your purchase!",
    dueDate: undefined,
    paymentStatus: "Paid" as unknown as PaymentStatus,
    status: "Paid" as unknown as InvoiceStatus,
    createdAt: now - oneDay * BigInt(2),
    updatedAt: now - oneDay,
  },
  {
    invoiceId: BigInt(2),
    invoiceNumber: "INV-0002",
    customerId: BigInt(2),
    customerName: "Rajesh Gupta",
    customerPhone: "9845012345",
    items: [
      { productId: BigInt(2), productName: "Silk Saree - Banarasi", quantity: BigInt(1), unitPrice: BigInt(449900), discount: BigInt(0), taxPercent: BigInt(18), lineTotal: BigInt(449900) },
    ],
    subtotal: BigInt(449900),
    cgst: BigInt(40491),
    sgst: BigInt(40491),
    discount: BigInt(0),
    total: BigInt(530882),
    notes: "GST Invoice - B2B",
    dueDate: now + oneDay * BigInt(7),
    paymentStatus: "Unpaid" as unknown as PaymentStatus,
    status: "Sent" as unknown as InvoiceStatus,
    createdAt: now - oneDay,
    updatedAt: now - oneDay,
  },
  {
    invoiceId: BigInt(3),
    invoiceNumber: "INV-0003",
    customerId: BigInt(3),
    customerName: "Anita Patel",
    customerPhone: "9765432109",
    items: [
      { productId: BigInt(3), productName: "Men's Formal Shirt - White", quantity: BigInt(1), unitPrice: BigInt(99900), discount: BigInt(500), taxPercent: BigInt(18), lineTotal: BigInt(99400) },
    ],
    subtotal: BigInt(99400),
    cgst: BigInt(8946),
    sgst: BigInt(8946),
    discount: BigInt(500),
    total: BigInt(117292),
    notes: undefined,
    dueDate: undefined,
    paymentStatus: "Unpaid" as unknown as PaymentStatus,
    status: "Draft" as unknown as InvoiceStatus,
    createdAt: now,
    updatedAt: now,
  },
];

export const mockBackend: backendInterface = {
  _initializeAccessControl: async () => undefined,
  assignCallerUserRole: async (_user: Principal, _role: UserRole) => undefined,
  createCustomer: async (input) => ({
    customerId: BigInt(100),
    name: input.name,
    phone: input.phone,
    email: input.email,
    address: input.address,
    gstNumber: input.gstNumber,
    totalPurchaseAmount: BigInt(0),
    pendingAmount: BigInt(0),
    createdAt: now,
  }),
  createInvoice: async (input) => ({
    ...input,
    invoiceId: BigInt(100),
    invoiceNumber: "INV-0100",
    createdAt: now,
    updatedAt: now,
    customerId: input.customerId,
    dueDate: input.dueDate,
    notes: input.notes,
  }),
  createProduct: async (input) => ({
    ...input,
    productId: BigInt(100),
    createdAt: now,
    barcode: input.barcode,
    imageUrl: input.imageUrl,
    supplierName: input.supplierName,
  }),
  deleteBusinessProfile: async () => true,
  deleteCustomer: async () => true,
  deleteInvoice: async () => true,
  deleteProduct: async () => true,
  getBusinessProfile: async () => sampleProfile,
  getCallerUserRole: async () => "admin" as unknown as UserRole,
  getCustomer: async (id) => sampleCustomers.find(c => c.customerId === id) ?? null,
  getDashboardStats: async () => dashboardStats,
  getInvoice: async (id) => sampleInvoices.find(i => i.invoiceId === id) ?? null,
  getInvoicesByCustomer: async () => sampleInvoices,
  getInvoicesByStatus: async () => sampleInvoices,
  getLowStockProducts: async () => sampleProducts.filter(p => p.quantity <= BigInt(5)),
  getProduct: async (id) => sampleProducts.find(p => p.productId === id) ?? null,
  getStockHistory: async (): Promise<StockUpdate[]> => [
    {
      updateId: BigInt(1),
      productId: BigInt(3),
      productName: "Men's Formal Shirt - White",
      previousQty: BigInt(10),
      newQty: BigInt(3),
      changeReason: "Sale - Invoice INV-0001",
      changedAt: now - oneDay,
    },
  ],
  initSeedData: async () => undefined,
  isCallerAdmin: async () => true,
  listCustomers: async (): Promise<PaginatedResult_2> => ({
    total: BigInt(sampleCustomers.length),
    page: BigInt(1),
    pageSize: BigInt(10),
    items: sampleCustomers,
  }),
  listInvoices: async (): Promise<PaginatedResult_1> => ({
    total: BigInt(sampleInvoices.length),
    page: BigInt(1),
    pageSize: BigInt(10),
    items: sampleInvoices,
  }),
  listProducts: async (): Promise<PaginatedResult> => ({
    total: BigInt(sampleProducts.length),
    page: BigInt(1),
    pageSize: BigInt(10),
    items: sampleProducts,
  }),
  saveBusinessProfile: async (input) => ({ ...input, businessId: "aaaaa-aa" as unknown as Principal, logoUrl: input.logoUrl, gstNumber: input.gstNumber }),
  updateBusinessProfile: async (input) => ({ ...input, businessId: "aaaaa-aa" as unknown as Principal, logoUrl: input.logoUrl, gstNumber: input.gstNumber }),
  updateCustomer: async (id, input) => ({
    customerId: id,
    name: input.name,
    phone: input.phone,
    email: input.email,
    address: input.address,
    gstNumber: input.gstNumber,
    totalPurchaseAmount: BigInt(0),
    pendingAmount: BigInt(0),
    createdAt: now,
  }),
  updateInvoice: async (id, input) => ({
    ...input,
    invoiceId: id,
    invoiceNumber: "INV-0001",
    createdAt: now,
    updatedAt: now,
    customerId: input.customerId,
    dueDate: input.dueDate,
    notes: input.notes,
  }),
  updateInvoicePaymentStatus: async (id, paymentStatus) => ({
    ...sampleInvoices[0],
    invoiceId: id,
    paymentStatus,
  }),
  updateProduct: async (id, input) => ({
    ...input,
    productId: id,
    createdAt: now,
    barcode: input.barcode,
    imageUrl: input.imageUrl,
    supplierName: input.supplierName,
  }),
  updateProductStock: async (id, newQty, reason): Promise<StockUpdate | null> => ({
    updateId: BigInt(1),
    productId: id,
    productName: "Product",
    previousQty: BigInt(0),
    newQty,
    changeReason: reason,
    changedAt: now,
  }),
};
